select distinct(jobTitle) from employees;


-- we can use like for string comparison

select * from employees where jobTitle like "Sales%"

--the wild card must be something

-- find all employees whose job title has 

